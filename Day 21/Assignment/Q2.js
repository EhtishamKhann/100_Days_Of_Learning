// Q2. Create a new array using the map function whose each element is equal to the
// original element plus 5.

let numbers = [1, 2, 3, 4, 5];

let plusFive = numbers.map( (n) => n + 5 );

console.log(plusFive);
