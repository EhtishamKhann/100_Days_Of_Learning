// Q3. Create a new array whose elements are in uppercase of words present in the
// original array

let lowercase = [ 'a', 'b', 'c', 'd', 'e'];

let uppercase = lowercase.map( (char) => char.toUpperCase());
console.log(uppercase);
